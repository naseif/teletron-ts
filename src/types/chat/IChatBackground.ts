import { IDocument } from "../media";
import { BackgroundFillType, BackgroundType } from "../misc";

/**
 * This object represents a chat background.
 */
export interface IChatBackground {
  /**
   * Type of the background
   */
  type: BackgroundType;
}
